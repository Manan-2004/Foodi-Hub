export interface TopFood {
    image: string;
    title: string;
    description: string;
    rating: string;
}

export interface Feature {
    icon: string;
    title: string;
    description: string;
}